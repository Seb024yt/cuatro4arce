Si se traspasa este codigo
una vez descargado tienes que eliminar los caches y eliminar el .venv

Paso 1 - Eliminar caches
Paso 2 - Eliminar .venv 
Paso 3 - descargar los requirements con terminal 

    python -m pip install -r requirements.txt
    python -m playwright install
    
